---
apiVersion: helm.integrations.flux.weave.works/v1alpha2
kind: FluxHelmRelease
metadata:
  name: angular
  namespace: default
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.angularReservationUI: glob:2.0.*
spec:
  chartGitPath: angular
  releaseName: angular
  values:
    environment: default

    angularReservationUI:
      replicaCount: 1
      image: bmwreferencerepository.azurecr.io/angular-reservation-ui:2.0.184
